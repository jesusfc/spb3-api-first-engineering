type: object
properties:
  customerId:
    type: string
    format: uuid
  selectPaymentMethodId:
    type: string
    format: uuid
  orderLines:
    type: array
    items:
      $ref: order_line_patch.yaml
    description: The order lines of the order